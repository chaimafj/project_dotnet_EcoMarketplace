ClientApp (Angular) starter

Quick start:

1. cd ClientApp
2. npm install
3. npm start

The dev server uses a proxy to forward `/api` calls to `http://localhost:5000` (check `proxy.conf.json`).

This is a minimal starter with two components: product list and product form.
